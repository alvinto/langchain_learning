# 核心
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0
langchain-text-splitters>=0.3.0

# Agent / LangGraph
langgraph>=0.2.0

# RAG
faiss-cpu>=1.8.0
pypdf>=4.0.0
beautifulsoup4>=4.12.0
unstructured>=0.15.0

# 工具
python-dotenv>=1.0.0
pydantic>=2.0.0
tiktoken>=0.7.0

# Deep Research（09 章用）
tavily-python>=0.5.0     # 主搜索后端（需 TAVILY_API_KEY；没填会自动降级到 DDG）
ddgs>=6.0.0              # DuckDuckGo 兜底搜索，免 key
trafilatura>=1.12.0      # URL → 正文抽取
requests>=2.31.0         # fetch 同步兜底
httpx>=0.27.0            # fetch async 主路径（researcher 真并行靠它）
